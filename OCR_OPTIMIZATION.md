# OCR 全屏截图识别优化说明

## 问题描述

在使用小区域框选截图时，遗物识别效果基本完美；但使用全屏截图时，识别效果明显下降，表现为：
- 部分文字识别错误
- OCR结果无法通过纠错过滤，导致最终无结果
- **关键症状**：单个物品截图可以识别，但全屏或大图就识别不到

## 根本原因分析

### 🔴 核心问题：RapidOCR检测器过度缩小图像（最关键！）

**发现**：RapidOCR的默认检测配置 `det_limit_side_len=736` 会将图像的**最小边**强制缩放到736px。

**影响**：
- **2560×1440的全屏截图** → 被缩小到约 **1312×736** 进行检测
- **文字像素密度大幅降低**，小字体文字变得模糊不清
- **文本检测器无法定位到文字区域**，导致返回0个检测结果

**对比**：
- **小区域截图**（如400×300）：最小边已经小于736，不会被缩小，所以识别效果好
- **全屏截图**（2560×1440）：最小边1440被缩小到736（缩小51%），文字细节丢失严重

### 1. 上采样策略不合理
**原逻辑**：固定阈值 `宽度 > 900px` 时放大 `1.4倍`

**问题**：
- **小区域截图**（400-600px宽）：不触发上采样，但因为区域小、文字占比大，识别效果好
- **全屏截图**（2560×1440或1920×1080）：虽然触发1.4倍放大，但相对于原始尺寸，放大倍数不足，文字像素密度仍然偏低

### 2. 缺少图像增强预处理
全屏截图中游戏UI元素背景复杂（渐变、特效、半透明等），原始的简单灰度化处理无法有效突出文字边缘。

### 3. RapidOCR参数固定
无论图像大小都使用相同的 `text_score=0.38, box_thresh=0.22`，大图场景下应该使用更宽松的阈值以提高召回率。

---

## 优化方案

### 🔴 优化0：修复RapidOCR检测器limit_side_len参数（最关键！）

**文件**：`recognizers/base_ocr.py`, `core/constants.py`

**问题根源**：
```python
# RapidOCR默认配置
det_limit_side_len = 736  # 将图像最小边限制为736px
det_limit_type = "min"    # 按最小边缩放
```

**修复方案**：
```python
# 新增配置
OCR_DET_LIMIT_SIDE_LEN = 960  # 提高到960，保留更多文字细节
OCR_DET_LIMIT_TYPE = "min"    # 保持宽高比
```

**效果对比**：
| 图像尺寸 | 默认736处理后 | 优化后960处理后 | 改善 |
|---------|-------------|---------------|------|
| 2560×1440 | 1312×736 | 1707×960 | **+30% 像素** |
| 1920×1080 | 985×736 | 1280×960 | **+30% 像素** |
| 1280×720 | 1034×736 | 1280×960 | **+24% 像素** |
| 640×480 | 640×480 (不变) | 640×480 (不变) | 无影响 |

**代码实现**：
```python
def __init__(self):
    self._ocr = RapidOCR(
        text_score=OCR_TEXT_SCORE, 
        box_thresh=OCR_BOX_THRESH,
        det_limit_side_len=OCR_DET_LIMIT_SIDE_LEN,  # ← 关键修复
        det_limit_type=OCR_DET_LIMIT_TYPE
    )
```

### 优化1：自适应上采样策略

**文件**：`recognizers/base_ocr.py`, `core/constants.py`

**新增配置**：
```python
OCR_ADAPTIVE_UPSCALE_ENABLED = True      # 启用自适应上采样
OCR_ADAPTIVE_BASE_WIDTH = 1200           # 基准宽度阈值
OCR_ADAPTIVE_MAX_SCALE = 2.5             # 最大上采样倍数
OCR_ADAPTIVE_MIN_SCALE = 1.2             # 最小上采样倍数
```

**新算法**（`_calculate_adaptive_scale`）：
- **小图**（宽 < 900px）：不放大（1.0x），保持原始质量
- **中图**（900-1920px）：线性插值 1.4x ~ 1.8x
- **大图**（> 1920px）：动态计算，范围 1.2x ~ 2.5x，确保文字像素密度足够

**示例**：
- 1920px 宽度 → 约 1.8x 放大
- 2560px 宽度 → 约 2.0x 放大
- 3840px 宽度 → 约 2.5x 放大（上限）

### 优化2：对比度增强（CLAHE）

**文件**：`recognizers/base_ocr.py`

**新增配置**：
```python
OCR_ENHANCE_CONTRAST = True              # 启用对比度增强
OCR_CLAHE_CLIP_LIMIT = 2.0               # CLAHE对比度限制
OCR_CLAHE_GRID_SIZE = 8                  # CLAHE网格大小
```

**实现**（`_enhance_contrast`）：
- 转换到 LAB 色彩空间
- 对 L（亮度）通道应用 CLAHE（Contrast Limited Adaptive Histogram Equalization）
- 转回 BGR 色彩空间

**效果**：
- 提高文字与背景的对比度
- 保留局部细节，避免全局直方图均衡化的过曝问题
- 仅对上采样倍数 >= 1.5x 的图像应用（即主要针对大图/全屏截图）

### 优化3：动态RapidOCR参数配置

**文件**：`recognizers/base_ocr.py`, `core/constants.py`

**新增配置**：
```python
OCR_DYNAMIC_PARAMS_ENABLED = True        # 启用动态参数调整
OCR_LARGE_IMAGE_TEXT_SCORE = 0.35        # 大图文本置信度（降低以提高召回）
OCR_LARGE_IMAGE_BOX_THRESH = 0.20        # 大图检测框阈值（降低以检测更多候选）
OCR_LARGE_IMAGE_THRESHOLD = 1500         # 判定为大图的宽度阈值
```

**实现**（`_get_ocr_engine`）：
- **标准引擎**：`text_score=0.38, box_thresh=0.22`（适用于小图/中图）
- **大图优化引擎**：`text_score=0.35, box_thresh=0.20`（延迟初始化，适用于宽度 > 1500px 的图像）
- 根据输入图像宽度自动选择合适的引擎

**原理**：
- 降低 `text_score`：允许置信度较低的文本通过，提高召回率
- 降低 `box_thresh`：检测到更多的文本框候选，减少漏检

---

## 性能影响

### 预期开销
1. **检测器参数调整**：无额外开销（仅改变RapidOCR内部处理逻辑）
2. **自适应上采样**：大图可能需要更高的放大倍数（最高2.5x），增加OCR处理时间约 20-40%
3. **对比度增强**：CLAHE操作增加约 5-10ms 预处理时间
4. **动态参数**：无额外开销（延迟初始化，仅在需要时创建第二个OCR引擎）

### 优化建议
- 对于**价格查询4等分截图**（推荐方式）：仍使用原有逻辑，不受影响
- 对于**全屏截图**：接受略高的处理时间换取更好的识别率
- 用户应优先使用**框选区域**而非全屏截图（性能最佳）

---

## 调试功能

为了帮助诊断OCR识别问题，代码已添加自动调试图像保存功能。

### 调试图像保存

每次OCR识别时，会自动保存以下图像到 `debug/` 目录：

1. **原始输入图像**：`ocr_input_{timestamp}_original.png`
   - 保存传给OCR前的原始截图
   - 可用于检查截图质量、文字清晰度

2. **预处理后图像**：`ocr_input_{timestamp}_preprocessed.png`
   - 保存经过上采样和对比度增强后的图像
   - 可用于检查预处理效果是否合理

### 调试日志输出

优化后的代码会输出详细日志：

```
[OCR调试] 原始图像已保存: 2560x1440
[OCR] 图像尺寸 2560x1440, 上采样倍数 2.00x
[OCR] 已应用对比度增强
[OCR调试] 预处理后图像已保存: 5120x2880
[OCR调试] 检测器配置: {'limit_side_len': 960, 'limit_type': 'min'}
[OCR调试] 原始检测结果: 8 个文本框
[OCR] 识别完成，使用大图优化引擎
[OCR] 8 条结果, 1250ms
```

### 诊断步骤

如果仍然遇到识别问题：

1. **检查debug目录**：查看保存的图像文件
2. **观察日志**：确认检测器配置是否正确（limit_side_len应为960）
3. **检查原始检测结果数**：如果为0，说明检测器未能定位到文字
4. **对比两张图像**：确认预处理是否过度模糊或失真

---

## 测试建议

### 测试场景
1. **小区域框选**（400-600px宽）：验证优化不影响原有良好表现
2. **中等区域**（900-1500px宽）：验证线性插值上采样效果
3. **全屏截图**（1920×1080 / 2560×1440）：重点测试识别率提升

### 验证指标
- 识别成功率（成功匹配的物品数 / 总物品数）
- 误报率（错误识别的数量）
- 平均处理时间（ms）

---

## 回滚方案

如需禁用某项优化，修改 `core/constants.py` 中的对应开关：

```python
# 恢复默认检测器参数（可能导致大图识别失败）
OCR_DET_LIMIT_SIDE_LEN = 736  # RapidOCR默认值

# 禁用其他优化
OCR_ADAPTIVE_UPSCALE_ENABLED = False  # 禁用自适应上采样，恢复固定1.4x
OCR_ENHANCE_CONTRAST = False          # 禁用对比度增强
OCR_DYNAMIC_PARAMS_ENABLED = False    # 禁用动态参数，统一使用标准引擎
```

---

## 相关文件清单

| 文件 | 修改内容 |
|------|---------|
| `core/constants.py` | 新增检测器参数、自适应上采样、图像增强、动态参数配置常量 |
| `recognizers/base_ocr.py` | 实现 `_calculate_adaptive_scale()`, `_enhance_contrast()`, `_get_ocr_engine()`，重构 `_run_ocr()` 添加调试功能 |
| `OCR_OPTIMIZATION.md` | 本文档，记录所有优化方案和诊断方法 |
| `debug/` | 自动创建的调试目录，保存OCR输入图像 |

---

## 后续优化方向

如果当前优化仍不能满足需求，可考虑：

1. **超分辨率模型**：使用 ESRGAN/Real-ESRGAN 进行智能放大（性能开销较大）
2. **二值化预处理**：对特定颜色文字（如白色UI文字）进行阈值分割
3. **多尺度融合**：同时对原图和放大图执行OCR，合并结果
4. **自定义训练**：针对Warframe字体微调OCR模型
5. **调整det_db_thresh**：进一步降低文本检测的二值化阈值（需要修改RapidOCR源码或配置文件）
